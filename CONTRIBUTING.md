# Contributing

Use focused changes, add tests before implementation, and run the complete lint, typecheck, test, and build suite. Security-sensitive behavior requires negative tests. Public documentation follows the four Diátaxis categories in `docs/`.


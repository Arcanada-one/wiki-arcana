# Storage phase boundary

The initial release creates only access levels, knowledge spaces, hierarchy closure, grants, and effective permissions. It creates no content tables, vector columns, indexes, graph objects, or extensions.

Future graph and vector adapters exist as typed stubs and fail explicitly. Embeddings will be minted by the configured external embedding service; caller identity tokens will never be forwarded.

